{
  "name": "@relay-protocol/fixtures",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "lint": "lint"
  },
  "devDependencies": {
    "@relay-protocol/eslint-config": "workspace:*",
    "@relay-protocol/tsconfig": "workspace:*",
    "@relay-protocol/types": "workspace:*",
    "eslint": "9.36.0",
    "typescript": "5.9.3"
  }
}
