const code =
  '// src/common/bytes.ts\nfunction normalizeHex(hex, field = "hex") {\n  const clean = String(hex ?? "").replace(/^0x/i, "");\n  if (!/^[0-9a-fA-F]*$/.test(clean) || clean.length % 2 !== 0) {\n    throw new Error(`invalid hex for ${field}: ${hex}`);\n  }\n  return clean.toLowerCase();\n}\nfunction hexToBytes(hex, field = "hex") {\n  const clean = normalizeHex(hex, field);\n  const bytes = new Uint8Array(clean.length / 2);\n  for (let i = 0; i < bytes.length; i++) {\n    bytes[i] = parseInt(clean.slice(i * 2, i * 2 + 2), 16);\n  }\n  return bytes;\n}\nfunction bytesToHex(bytes) {\n  return Array.from(bytes).map((b) => b.toString(16).padStart(2, "0")).join("");\n}\nfunction concatBytes(...arrays) {\n  const total = arrays.reduce((sum, arr) => sum + arr.length, 0);\n  const out = new Uint8Array(total);\n  let offset = 0;\n  for (const arr of arrays) {\n    out.set(arr, offset);\n    offset += arr.length;\n  }\n  return out;\n}\nfunction equalBytes(a, b) {\n  if (a.length !== b.length) return false;\n  for (let i = 0; i < a.length; i++) if (a[i] !== b[i]) return false;\n  return true;\n}\nfunction bytesToBigInt(bytes) {\n  let n = 0n;\n  for (const b of bytes) n = (n << 8n) + BigInt(b);\n  return n;\n}\nfunction bigIntToBytes(value) {\n  if (value === 0n) return new Uint8Array(0);\n  let hex = value.toString(16);\n  if (hex.length % 2) hex = `0${hex}`;\n  return hexToBytes(hex, "quantity");\n}\nfunction expectLength(bytes, length, field) {\n  if (bytes.length !== length) throw new Error(`${field} must be exactly ${length} bytes`);\n  return bytes;\n}\n\n// src/common/crypto.ts\nimport { keccak_256 } from "https://cdn.jsdelivr.net/npm/@noble/hashes@1.8.0/sha3/+esm";\nvar DOMAIN_SEPARATOR = "relay-allocator";\nvar encoder = new TextEncoder();\nfunction keccak(bytes) {\n  return new Uint8Array(keccak_256(bytes));\n}\nasync function hmacSha256(key, data) {\n  const cryptoKey = await crypto.subtle.importKey(\n    "raw",\n    key,\n    { name: "HMAC", hash: "SHA-256" },\n    false,\n    ["sign"]\n  );\n  const mac = await crypto.subtle.sign("HMAC", cryptoKey, data);\n  return new Uint8Array(mac);\n}\nasync function hkdfExtract(salt, ikm) {\n  return hmacSha256(salt, ikm);\n}\nasync function hkdfExpand(prk, info, length) {\n  const hashLen = 32;\n  const n = Math.ceil(length / hashLen);\n  const okm = new Uint8Array(n * hashLen);\n  let prev = new Uint8Array(0);\n  for (let i = 1; i <= n; i++) {\n    const input = new Uint8Array(prev.length + info.length + 1);\n    input.set(prev, 0);\n    input.set(info, prev.length);\n    input[prev.length + info.length] = i;\n    prev = await hmacSha256(prk, input);\n    okm.set(prev, (i - 1) * hashLen);\n  }\n  return new Uint8Array(okm.slice(0, length));\n}\nasync function deriveKey(pkpPrivateKeyHex, vmTypeInfo) {\n  const ikm = hexToBytes(pkpPrivateKeyHex, "pkpPrivateKey");\n  const salt = encoder.encode(DOMAIN_SEPARATOR);\n  const info = encoder.encode(vmTypeInfo);\n  const prk = await hkdfExtract(salt, ikm);\n  return hkdfExpand(prk, info, 32);\n}\n\n// src/common/abi.ts\nfunction parseQuantity(value, field) {\n  const s = String(value ?? "");\n  if (s.length === 0) throw new Error(`invalid quantity for ${field}`);\n  return BigInt(s);\n}\nfunction quantityToWord(value, field) {\n  const n = parseQuantity(value, field);\n  if (n < 0n || n >= 1n << 256n) throw new Error(`${field} does not fit uint256`);\n  const out = new Uint8Array(32);\n  let x = n;\n  for (let i = 31; i >= 0; i--) {\n    out[i] = Number(x & 0xffn);\n    x >>= 8n;\n  }\n  return out;\n}\nfunction u64ToWord(value) {\n  if (!Number.isSafeInteger(value) || value < 0) {\n    throw new Error(`invalid hashIndex: ${value}`);\n  }\n  const out = new Uint8Array(32);\n  let x = BigInt(value);\n  for (let i = 31; i >= 24; i--) {\n    out[i] = Number(x & 0xffn);\n    x >>= 8n;\n  }\n  return out;\n}\nfunction padRight32(bytes) {\n  const len = Math.ceil(bytes.length / 32) * 32;\n  const out = new Uint8Array(len);\n  out.set(bytes);\n  return out;\n}\nfunction abiEncodeString(value) {\n  return abiEncodeBytes(new TextEncoder().encode(value));\n}\nfunction abiEncodeBytes(bytes) {\n  return concatBytes(quantityToWord(bytes.length, "bytes.length"), padRight32(bytes));\n}\nfunction abiEncodeWithdrawRequest(req) {\n  const dynamicParts = [\n    abiEncodeString(req.chainId),\n    abiEncodeBytes(hexToBytes(req.depository, "depository")),\n    abiEncodeBytes(hexToBytes(req.currency, "currency")),\n    null,\n    abiEncodeString(req.spenderChainId),\n    abiEncodeBytes(hexToBytes(req.spender, "spender")),\n    abiEncodeBytes(hexToBytes(req.receiver, "receiver")),\n    abiEncodeBytes(hexToBytes(req.data, "data")),\n    null\n  ];\n  const nonce = hexToBytes(req.nonce, "nonce");\n  if (nonce.length !== 32) throw new Error("withdraw request nonce must be exactly 32 bytes");\n  const head = [];\n  const tail = [];\n  let offset = 32 * 9;\n  for (let i = 0; i < 9; i++) {\n    if (i === 3) head.push(quantityToWord(req.amount, "amount"));\n    else if (i === 8) head.push(nonce);\n    else {\n      const part = dynamicParts[i];\n      if (!part) throw new Error(`missing ABI dynamic part at index ${i}`);\n      head.push(quantityToWord(offset, "offset"));\n      tail.push(part);\n      offset += part.length;\n    }\n  }\n  const tupleBody = concatBytes(...head, ...tail);\n  return concatBytes(quantityToWord(32, "tupleOffset"), tupleBody);\n}\nfunction computeWithdrawRequestHash(req) {\n  return keccak(abiEncodeWithdrawRequest(req));\n}\nfunction computeHashesToSignSlot(withdrawRequestHash, hashIndex) {\n  const hashesToSignSlot = new Uint8Array(32);\n  hashesToSignSlot[31] = 5;\n  const outer = keccak(concatBytes(withdrawRequestHash, hashesToSignSlot));\n  return keccak(concatBytes(u64ToWord(hashIndex), outer));\n}\n\n// src/config.ts\nvar ALLOCATOR_ENV = "dev";\nvar ALLOCATOR_ADDRESS = "0x357dfbec07a628e934bdb3642056fd72f10a7902";\nvar HUB_EVM_CHAIN_ID = Number.parseInt(421614, 10);\nvar ALLOWED_ORACLES = JSON.parse(\'["0xcda3c24706c1a5eea958a988693e8a838d520af9","0xf24a399259f47c6360d00da3793eca9cc6ad1caa"]\');\nvar ORACLE_SIGNATURE_THRESHOLD = Number.parseInt(\n  "2",\n  10\n);\nvar STORAGE_PROOF_METHOD = "canonical";\n\n// src/common/rlp.ts\nfunction rlpEncode(input) {\n  if (Array.isArray(input)) {\n    const payload = concatBytes(...input.map(rlpEncode));\n    return rlpEncodeLength(payload, 192);\n  }\n  const bytes = input;\n  if (bytes.length === 1 && bytes[0] < 128) return bytes;\n  return rlpEncodeLength(bytes, 128);\n}\nfunction rlpEncodeLength(payload, offset) {\n  if (payload.length <= 55) return concatBytes(new Uint8Array([offset + payload.length]), payload);\n  const lenBytes = bigIntToBytes(BigInt(payload.length));\n  return concatBytes(new Uint8Array([offset + 55 + lenBytes.length]), lenBytes, payload);\n}\nfunction rlpDecode(bytes) {\n  const [value, pos] = rlpDecodeAt(bytes, 0);\n  if (pos !== bytes.length) throw new Error("invalid RLP: trailing bytes");\n  return value;\n}\nfunction rlpDecodeAt(bytes, pos) {\n  if (pos >= bytes.length) throw new Error("invalid RLP: out of bounds");\n  const prefix = bytes[pos];\n  if (prefix < 128) return [bytes.slice(pos, pos + 1), pos + 1];\n  if (prefix <= 183) {\n    const len2 = prefix - 128;\n    return [bytes.slice(pos + 1, pos + 1 + len2), pos + 1 + len2];\n  }\n  if (prefix <= 191) {\n    const lenOfLen2 = prefix - 183;\n    const len2 = Number(bytesToBigInt(bytes.slice(pos + 1, pos + 1 + lenOfLen2)));\n    return [bytes.slice(pos + 1 + lenOfLen2, pos + 1 + lenOfLen2 + len2), pos + 1 + lenOfLen2 + len2];\n  }\n  if (prefix <= 247) {\n    const len2 = prefix - 192;\n    return rlpDecodeList(bytes, pos + 1, pos + 1 + len2);\n  }\n  const lenOfLen = prefix - 247;\n  const len = Number(bytesToBigInt(bytes.slice(pos + 1, pos + 1 + lenOfLen)));\n  return rlpDecodeList(bytes, pos + 1 + lenOfLen, pos + 1 + lenOfLen + len);\n}\nfunction rlpDecodeList(bytes, start, end) {\n  const list = [];\n  let pos = start;\n  while (pos < end) {\n    const [item, next] = rlpDecodeAt(bytes, pos);\n    list.push(item);\n    pos = next;\n  }\n  if (pos !== end) throw new Error("invalid RLP list");\n  return [list, end];\n}\nfunction asBytes(value, label) {\n  if (value instanceof Uint8Array) return value;\n  throw new Error(`${label} expected RLP bytes`);\n}\n\n// src/common/mpt.ts\nfunction bytesToNibbles(bytes) {\n  const out = [];\n  for (const b of bytes) out.push(b >> 4, b & 15);\n  return out;\n}\nfunction decodeCompactPath(bytes) {\n  const nibbles = bytesToNibbles(bytes);\n  const flag = nibbles[0] ?? 0;\n  const isLeaf = (flag & 2) !== 0;\n  const isOdd = (flag & 1) !== 0;\n  return { isLeaf, nibbles: isOdd ? nibbles.slice(1) : nibbles.slice(2) };\n}\nfunction verifyMptProof(rootHash, keyBytes, expectedValue, proofHexNodes, label) {\n  let expectedRef = rootHash;\n  const key = bytesToNibbles(keyBytes);\n  let keyPos = 0;\n  for (let proofIndex = 0; proofIndex < proofHexNodes.length; proofIndex++) {\n    const encodedNode = hexToBytes(proofHexNodes[proofIndex], `${label}.proof[${proofIndex}]`);\n    if (expectedRef.length === 32) {\n      const nodeHash = keccak(encodedNode);\n      if (!equalBytes(nodeHash, expectedRef)) throw new Error(`${label} proof node hash mismatch`);\n    } else if (!equalBytes(encodedNode, expectedRef)) {\n      throw new Error(`${label} embedded proof node mismatch`);\n    }\n    const node = rlpDecode(encodedNode);\n    if (!Array.isArray(node)) throw new Error(`${label} proof node is not a list`);\n    if (node.length === 17) {\n      if (keyPos === key.length) {\n        if (!equalBytes(asBytes(node[16], `${label} branch value`), expectedValue)) {\n          throw new Error(`${label} proof value mismatch`);\n        }\n        return;\n      }\n      expectedRef = asBytes(node[key[keyPos++]], `${label} branch child`);\n      if (expectedRef.length === 0) throw new Error(`${label} proof missing branch child`);\n    } else if (node.length === 2) {\n      const path = decodeCompactPath(asBytes(node[0], `${label} path`));\n      for (let i = 0; i < path.nibbles.length; i++) {\n        if (key[keyPos + i] !== path.nibbles[i]) throw new Error(`${label} proof path mismatch`);\n      }\n      keyPos += path.nibbles.length;\n      if (path.isLeaf) {\n        if (keyPos !== key.length) throw new Error(`${label} leaf ended before key`);\n        if (!equalBytes(asBytes(node[1], `${label} leaf value`), expectedValue)) {\n          throw new Error(`${label} proof value mismatch`);\n        }\n        return;\n      }\n      expectedRef = asBytes(node[1], `${label} extension child`);\n    } else {\n      throw new Error(`${label} invalid MPT node length: ${node.length}`);\n    }\n  }\n  throw new Error(`${label} proof ended before value was verified`);\n}\n\n// src/common/proofs.ts\nimport { addr, verifyTyped } from "https://cdn.jsdelivr.net/npm/micro-eth-signer@0.18.1/+esm";\nvar CANONICAL_BLOCK_EIP712_DOMAIN = {\n  name: "CanonicalHubBlock",\n  version: "1",\n  verifyingContract: "0x0000000000000000000000000000000000000000"\n};\nvar CANONICAL_BLOCK_EIP712_TYPES = {\n  CanonicalHubBlock: [\n    { name: "chainId", type: "uint256" },\n    { name: "blockNumber", type: "uint256" },\n    { name: "blockHash", type: "bytes32" },\n    { name: "stateRoot", type: "bytes32" }\n  ]\n};\nfunction encodeTrieAccount(proof) {\n  return rlpEncode([\n    bigIntToBytes(parseQuantity(proof.nonce, "nonce")),\n    bigIntToBytes(parseQuantity(proof.balance, "balance")),\n    expectLength(hexToBytes(proof.storageHash, "storageHash"), 32, "storageHash"),\n    expectLength(hexToBytes(proof.codeHash, "codeHash"), 32, "codeHash")\n  ]);\n}\nfunction rlpEncodeStorageValue(value) {\n  return rlpEncode(bigIntToBytes(parseQuantity(value, "storageProof.value")));\n}\nfunction normalizeAddress(address, field) {\n  return addr.addChecksum(`0x${bytesToHex(expectLength(hexToBytes(address, field), 20, field))}`);\n}\nfunction buildCanonicalBlockTypedData(canonicalBlock) {\n  return {\n    types: CANONICAL_BLOCK_EIP712_TYPES,\n    primaryType: "CanonicalHubBlock",\n    domain: {\n      ...CANONICAL_BLOCK_EIP712_DOMAIN,\n      chainId: canonicalBlock.block.chainId\n    },\n    message: {\n      chainId: parseQuantity(canonicalBlock.block.chainId, "canonicalBlock.block.chainId"),\n      blockNumber: parseQuantity(canonicalBlock.block.number, "canonicalBlock.block.number"),\n      blockHash: hexToBytes(canonicalBlock.block.hash, "canonicalBlock.block.hash"),\n      stateRoot: hexToBytes(canonicalBlock.block.stateRoot, "canonicalBlock.block.stateRoot")\n    }\n  };\n}\nfunction verifyCanonicalBlockSignatures(canonicalBlock, hubEvmChainId, allowedOracles, oracleSignatureThreshold) {\n  if (!canonicalBlock || typeof canonicalBlock !== "object") {\n    throw new Error("canonicalBlock is required");\n  }\n  if (!Array.isArray(canonicalBlock.signatures) || canonicalBlock.signatures.length === 0) {\n    throw new Error("canonicalBlock.signatures must not be empty");\n  }\n  if (canonicalBlock.block?.chainId !== hubEvmChainId) {\n    throw new Error(\n      `canonical block chainId mismatch: expected=${hubEvmChainId}, provided=${canonicalBlock.block?.chainId ?? ""}`\n    );\n  }\n  expectLength(hexToBytes(canonicalBlock.block.hash, "canonicalBlock.block.hash"), 32, "canonicalBlock.block.hash");\n  const stateRoot = expectLength(\n    hexToBytes(canonicalBlock.block.stateRoot, "canonicalBlock.block.stateRoot"),\n    32,\n    "canonicalBlock.block.stateRoot"\n  );\n  const typedData = buildCanonicalBlockTypedData(canonicalBlock);\n  const allowed = new Set(allowedOracles.map((oracle, index) => normalizeAddress(oracle, `allowedOracles[${index}]`)));\n  const verifiedSigners = /* @__PURE__ */ new Set();\n  for (const signature of canonicalBlock.signatures) {\n    const claimedOracle = normalizeAddress(signature.oracle, "canonicalBlock.signatures[].oracle");\n    if (!allowed.has(claimedOracle)) {\n      throw new Error(`oracle is not allowlisted: ${claimedOracle}`);\n    }\n    if (!verifyTyped(signature.signature, typedData, claimedOracle)) {\n      throw new Error(`canonical block signature mismatch for oracle ${claimedOracle}`);\n    }\n    verifiedSigners.add(claimedOracle);\n  }\n  if (verifiedSigners.size < oracleSignatureThreshold) {\n    throw new Error(\n      `canonical block signature threshold not met: required=${oracleSignatureThreshold}, verified=${verifiedSigners.size}`\n    );\n  }\n  return stateRoot;\n}\nfunction verifyCanonicalHashProof(withdrawRequest, hashIndex, proof, allocatorAddress, hubEvmChainId, allowedOracles, oracleSignatureThreshold) {\n  if (!proof || proof.type !== "canonical") throw new Error("proof type mismatch: expected canonical");\n  const stateRoot = verifyCanonicalBlockSignatures(\n    proof.canonicalBlock,\n    hubEvmChainId,\n    allowedOracles,\n    oracleSignatureThreshold\n  );\n  const withdrawRequestHash = computeWithdrawRequestHash(withdrawRequest);\n  const expectedSlot = computeHashesToSignSlot(withdrawRequestHash, hashIndex);\n  const proofSlot = expectLength(hexToBytes(proof.storageProof.key, "storageProof.key"), 32, "storageProof.key");\n  if (!equalBytes(proofSlot, expectedSlot)) {\n    throw new Error(\n      `storage proof key does not match requested withdrawRequest/hashIndex slot: expected=0x${bytesToHex(expectedSlot)}, provided=0x${bytesToHex(proofSlot)}`\n    );\n  }\n  const accountKey = keccak(expectLength(hexToBytes(allocatorAddress, "allocatorAddress"), 20, "allocatorAddress"));\n  verifyMptProof(stateRoot, accountKey, encodeTrieAccount(proof), proof.accountProof ?? [], "account");\n  const storageRoot = expectLength(hexToBytes(proof.storageHash, "storageHash"), 32, "storageHash");\n  const storageKey = keccak(expectedSlot);\n  verifyMptProof(\n    storageRoot,\n    storageKey,\n    rlpEncodeStorageValue(proof.storageProof.value),\n    proof.storageProof.proof ?? [],\n    "storage"\n  );\n  const word = quantityToWord(proof.storageProof.value, "storageProof.value");\n  return { withdrawRequestHash, hash: word };\n}\nfunction verifySovereignHashProof(_proof) {\n  throw new Error("sovereign storage proofs are not implemented in this bundle yet");\n}\nfunction verifyHashRequests({\n  withdrawRequest,\n  hashRequests,\n  allocatorAddress,\n  hubEvmChainId,\n  allowedOracles,\n  oracleSignatureThreshold\n}) {\n  if (!Array.isArray(hashRequests) || hashRequests.length === 0) {\n    throw new Error("hashRequests must not be empty");\n  }\n  return hashRequests.map((item) => {\n    if (STORAGE_PROOF_METHOD === "canonical") {\n      if (item.proof.type !== "canonical") {\n        throw new Error(`proof type mismatch: expected ${STORAGE_PROOF_METHOD}`);\n      }\n      const verified = verifyCanonicalHashProof(\n        withdrawRequest,\n        item.hashIndex,\n        item.proof,\n        allocatorAddress,\n        hubEvmChainId,\n        allowedOracles,\n        oracleSignatureThreshold\n      );\n      return {\n        hashIndex: item.hashIndex,\n        withdrawRequestHash: verified.withdrawRequestHash,\n        hash: verified.hash\n      };\n    }\n    if (item.proof.type !== "sovereign") {\n      throw new Error(`proof type mismatch: expected ${STORAGE_PROOF_METHOD}`);\n    }\n    verifySovereignHashProof(item.proof);\n  });\n}\n\n// src/vm/ethereum-vm.ts\nimport { addr as addr2 } from "https://cdn.jsdelivr.net/npm/micro-eth-signer@0.18.1/+esm";\nimport { sign as secpSign } from "https://cdn.jsdelivr.net/npm/micro-eth-signer@0.18.1/utils.js/+esm";\nasync function deriveEthereumPrivateKeyHex(pkpPrivateKeyHex) {\n  return bytesToHex(await deriveKey(pkpPrivateKeyHex, "ethereum-vm"));\n}\nfunction ethereumAddressFromPrivateKey(privateKeyHex) {\n  return addr2.fromPrivateKey(`0x${privateKeyHex}`);\n}\nfunction signDigest(messageHash, privateKeyHex) {\n  const recovered = secpSign(\n    messageHash,\n    hexToBytes(`0x${privateKeyHex}`)\n  ).toBytes("recovered");\n  const signature = new Uint8Array(65);\n  signature.set(recovered.slice(1), 0);\n  signature[64] = recovered[0] === 0 ? 27 : 28;\n  return `0x${bytesToHex(signature)}`;\n}\nasync function main({\n  pkpId,\n  action,\n  withdrawRequest,\n  hashRequests\n}) {\n  const pkpPrivateKey = await Lit.Actions.getPrivateKey({ pkpId });\n  const ethereumPrivateKey = await deriveEthereumPrivateKeyHex(pkpPrivateKey);\n  if (action === "wallet") {\n    return {\n      vmType: "ethereum-vm",\n      address: ethereumAddressFromPrivateKey(ethereumPrivateKey)\n    };\n  }\n  if (action === "sign") {\n    if (!withdrawRequest) throw new Error("withdrawRequest is required");\n    if (!hashRequests) throw new Error("hashRequests is required");\n    const verified = verifyHashRequests({\n      withdrawRequest,\n      hashRequests,\n      allocatorAddress: ALLOCATOR_ADDRESS,\n      hubEvmChainId: HUB_EVM_CHAIN_ID,\n      allowedOracles: ALLOWED_ORACLES,\n      oracleSignatureThreshold: ORACLE_SIGNATURE_THRESHOLD\n    });\n    return {\n      vmType: "ethereum-vm",\n      address: ethereumAddressFromPrivateKey(ethereumPrivateKey),\n      withdrawRequestHash: `0x${bytesToHex(verified[0].withdrawRequestHash)}`,\n      results: verified.map((item) => ({\n        hashIndex: item.hashIndex,\n        hash: `0x${bytesToHex(item.hash)}`,\n        signature: signDigest(item.hash, ethereumPrivateKey)\n      }))\n    };\n  }\n  throw new Error(`unknown action: ${action}`);\n}\n'

export default code
