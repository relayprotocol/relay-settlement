name: Publish Networks Package

on:
  push:
    branches:
      - main
    paths:
      - "packages/networks/**"
      - "packages/sdk/**"

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          registry-url: "https://registry.npmjs.org"

      - name: Install dependencies
        run: yarn

      - name: Build packages
        run: yarn build

      - name: Publish to npm
        run: |
          cd packages/networks
          npm publish
